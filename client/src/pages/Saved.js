import React from "react"

const Saved = props => (
  <div id="saved">
    <h4>Saved Articles</h4>
    <div>
      {/* <button id="btnSaved">Save</button> */}
    </div>
  </div>
);

export default Saved;